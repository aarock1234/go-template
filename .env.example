# Copy this file to .env and fill in your values.
# When running the app directly on your host (go run, goose, etc.),
# use localhost:5432 — Docker exposes the postgres container on that port.
DATABASE_URL=postgres://postgres:postgres@localhost:5432/app?sslmode=disable
